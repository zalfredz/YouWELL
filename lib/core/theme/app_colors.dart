import 'package:flutter/material.dart';

// Legacy palette used by the public web landing and recap pages.
const ink = Color(0xff203d34);
const green = Color(0xff42745b);
const cream = Color(0xfff6f7f2);
const muted = Color(0xff78857c);
const peach = Color(0xfff1b992);

// Shared product palette for the native app. These values match the web
// workspace so moving between desktop and mobile feels like one product.
const appCanvas = Color(0xff0d0e11);
const appSurface = Color(0xff16181d);
const appRaised = Color(0xff1c1f25);
const appBorder = Color(0xff2a2d34);
const appText = Color(0xfff1f3f5);
const appMuted = Color(0xff9298a3);
const appAccent = Color(0xff78e3b1);
const appAccentCyan = Color(0xff77d7e5);
const appAccentAmber = Color(0xffffc875);
